{
  "name": "@kaspa-x402/demo-gateway",
  "version": "1.0.0-rc.1",
  "private": true,
  "description": "Private Cloudflare Worker for the Kaspa x402 testnet gateway.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "npm --workspace @kaspa-x402/server run build && npm run build:self",
    "build:self": "wrangler types --config wrangler.jsonc && tsc -p tsconfig.json --noEmit && wrangler deploy --dry-run --outdir dist --config wrangler.jsonc",
    "test": "npm run build && npm run test:self",
    "test:self": "vitest run",
    "types:check": "wrangler types --config wrangler.jsonc --check"
  },
  "dependencies": {
    "@kaspa-x402/core": "1.0.0-rc.1",
    "@kaspa-x402/covenant": "1.0.0-rc.1",
    "@kaspa-x402/server": "1.0.0-rc.1",
    "@noble/curves": "^2.2.0",
    "@noble/hashes": "^2.2.0"
  }
}
